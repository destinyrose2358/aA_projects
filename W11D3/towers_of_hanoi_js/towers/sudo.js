function Game() {
  
}

Game.prototype.run = function () {
  /*
    until a nonstarting tower is full
    prompt user for a move
    attempt to make that move
      *if made check to see if they won
  */
};